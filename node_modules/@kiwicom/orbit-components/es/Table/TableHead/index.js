import * as React from "react";
import styled from "styled-components";
import defaultTheme from "../../defaultTheme";
import { StyledTableCell } from "../TableCell/index";
const StyledTableHead = styled(({
  children,
  className
}) => React.createElement("thead", {
  className: className
}, children)).withConfig({
  displayName: "TableHead__StyledTableHead",
  componentId: "sc-17bka47-0"
})(["border-bottom:1px solid ", ";width:100%;white-space:nowrap;", "{font-weight:", ";}"], ({
  theme
}) => theme.orbit.borderColorTableHead, StyledTableCell, ({
  theme
}) => theme.orbit.fontWeightTableHead);
StyledTableHead.defaultProps = {
  theme: defaultTheme
};

const TableHead = props => React.createElement(StyledTableHead, null, props.children);

export default TableHead;