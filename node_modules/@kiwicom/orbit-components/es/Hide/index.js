import * as React from "react";
import styled, { css } from "styled-components";
import mediaQueries from "../utils/mediaQuery";
import { DEVICES } from "../utils/mediaQuery/consts";
import defaultTheme from "../defaultTheme";

const getDisplay = () => ({
  block
}) => block ? "block" : "inline-block";

const StyledHide = styled.span.withConfig({
  displayName: "Hide__StyledHide",
  componentId: "ji2pgq-0"
})(["", ";"], ({
  on
}) => DEVICES.map(viewport => viewport in mediaQueries ? css(["", ";"], mediaQueries[viewport](css(["display:", ";"], on.indexOf(viewport) !== -1 ? "none" : getDisplay()))) : // "smallMobile" is not media query so we need to check it explicitly
viewport === "smallMobile" && on.indexOf(viewport) !== -1 && css(["display:none;"])));
StyledHide.defaultProps = {
  theme: defaultTheme
};

const Hide = ({
  on = [],
  block,
  children
}) => React.createElement(StyledHide, {
  on: on,
  block: block
}, children);

export default Hide;