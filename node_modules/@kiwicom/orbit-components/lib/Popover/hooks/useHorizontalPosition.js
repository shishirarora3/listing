"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _react = require("react");

var _consts = require("../consts");

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var useHorizontalPosition = function useHorizontalPosition(desiredAnchor, positions) {
  var _useState = (0, _react.useState)("start"),
      _useState2 = _slicedToArray(_useState, 2),
      anchor = _useState2[0],
      setAnchor = _useState2[1];

  (0, _react.useEffect)(function () {
    var canBeAnchorLeft = positions.containerLeft + positions.popoverWidth < positions.windowWidth;
    var canBeAnchorRight = positions.containerLeft + positions.containerWidth >= positions.popoverWidth; // returns the position name if the position can be set

    var isInside = function isInside(p) {
      if (p === _consts.ANCHORS.START && canBeAnchorLeft) {
        return _consts.ANCHORS.START;
      }

      if (p === _consts.ANCHORS.END && canBeAnchorRight) {
        return _consts.ANCHORS.END;
      }

      return false;
    };

    var possibleAnchor = desiredAnchor.map(function (p) {
      return isInside(p);
    }).filter(function (p) {
      return typeof p === "string";
    });
    var posAnchor = possibleAnchor[0];

    if (typeof posAnchor === "string") {
      setAnchor(posAnchor);
    }
  });
  return anchor;
};

var _default = useHorizontalPosition;
exports.default = _default;