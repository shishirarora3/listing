"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _react = require("react");

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var useDimensions = function useDimensions(_ref) {
  var containerRef = _ref.containerRef,
      popover = _ref.popover,
      content = _ref.content;

  var _useState = (0, _react.useState)({
    containerTop: 0,
    containerLeft: 0,
    containerHeight: 0,
    containerWidth: 0,
    popoverHeight: 0,
    popoverWidth: 0,
    windowWidth: 0,
    windowHeight: 0,
    contentHeight: 0
  }),
      _useState2 = _slicedToArray(_useState, 2),
      positions = _useState2[0],
      setPositions = _useState2[1];

  var calculate = function calculate() {
    if (containerRef && popover && content && typeof window !== "undefined") {
      var containerDimensions = containerRef.getBoundingClientRect(); // props.containerRef is passed with .current

      var popoverDimensions = popover.current.getBoundingClientRect();
      var contentHeight = content.current && content.current.getBoundingClientRect().height;
      setPositions({
        containerTop: containerDimensions.top,
        containerLeft: containerDimensions.left,
        containerHeight: containerDimensions.height,
        containerWidth: containerDimensions.width,
        popoverHeight: popoverDimensions.height,
        popoverWidth: popoverDimensions.width,
        windowWidth: window.innerWidth,
        windowHeight: window.innerHeight,
        contentHeight: contentHeight
      });
    }
  };

  (0, _react.useEffect)(function () {
    calculate();
    window.addEventListener("resize", calculate);
    return function () {
      window.removeEventListener("resize", calculate);
    };
  }, []);
  return positions;
};

var _default = useDimensions;
exports.default = _default;