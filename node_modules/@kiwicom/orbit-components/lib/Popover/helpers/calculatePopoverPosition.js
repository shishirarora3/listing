"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _consts = require("../consts");

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance"); }

function _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }

var calculatePopoverPosition = function calculatePopoverPosition(preferredPosition) {
  var mappedPositions = Object.keys(_consts.POSITIONS).map(function (k) {
    return _consts.POSITIONS[k];
  });
  var mappedAnchors = Object.keys(_consts.ANCHORS).map(function (k) {
    return _consts.ANCHORS[k];
  });
  return [[preferredPosition].concat(_toConsumableArray(mappedPositions.filter(function (p) {
    return p !== preferredPosition;
  }))), mappedAnchors];
};

var _default = calculatePopoverPosition;
exports.default = _default;